[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "deep-agentic-core-mcp"
version = "0.2.0"
description = "Unified MCP server for AgenticLens and Agentic Chaos workflows."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "DeepAgentLabs" }]
keywords = ["mcp", "agentic-ai", "observability", "chaos-engineering", "llm"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
  "jsonschema>=4.23,<5",
  "mcp>=1.10.0",
  "pydantic>=2.0,<3",
  "referencing>=0.35,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "mypy>=1.10",
  "types-jsonschema>=4.23",
  "build>=1.2",
  "twine>=5.1",
]

[project.scripts]
deep-agentic-core-mcp = "deep_agentic_core_mcp.server:main"

[project.urls]
Homepage = "https://github.com/DeepAgentLabs/mcp-server"
Repository = "https://github.com/DeepAgentLabs/mcp-server"
Issues = "https://github.com/DeepAgentLabs/mcp-server/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/deep_agentic_core_mcp"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N"]
ignore = ["B008"]

[tool.ruff.lint.isort]
known-first-party = ["deep_agentic_core_mcp"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.10"
strict = true
packages = ["deep_agentic_core_mcp"]
mypy_path = "src"

[[tool.mypy.overrides]]
module = [
  "agenticlens.*",
  "agentic_chaos.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
asyncio_mode = "auto"
